# Virtual environment
venv/

# Environment variables (API keys)
.env

# Python cache
__pycache__/
*.pyc

# macOS
.DS_Store

# PDFs - NEVER commit these (download from arXiv at runtime)
data/*.pdf

# Training data (contains paper excerpts - users should generate their own)
evaluation/training_pairs.json
evaluation/training_pairs_*.json
evaluation/chunks_for_qa.json

# Vector database - NOW COMMITTED (embeddings are okay to share)
# chroma_db/

# Model weights - NOW COMMITTED (fine-tuned embeddings)
# models/fine-tuned-embeddings/

# Checkpoints
checkpoints/

# Jupyter
*.ipynb_checkpoints/

# Claude instructions
CLAUDE.md