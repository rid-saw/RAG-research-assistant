[project]
name = "universal-rag-frontend"
version = "0.1.0"
description = "NiceGUI frontend for the Universal RAG backend"
requires-python = ">=3.11"
dependencies = [
    "nicegui>=2.0",
    "requests>=2.32",
]
