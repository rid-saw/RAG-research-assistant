[project]
name = "universal-rag-frontend"
version = "0.1.0"
description = "Streamlit frontend for the Universal RAG backend"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.40,<1.50",
    "requests>=2.32",
    "fpdf2>=2.8",
]
